(30pts) Write the instructions that will separate the Users information into a different collection

first you would create a users collection that outputs only unique users
by using $group, $first, $out

$group - this would group the tweets based on the primary key user_ID so every specific user only appears once

$first - this keeps only the first attribute of the instance

$out - this creates a new users collection which would be the result of the seperation

then with tweets only you would want to reference the users by ID 

using $project

project creates a primary key and removes user entirely 

and $out writes specifically to the tweets only collection




